<%= form_with(model: ticket, local: true) do |form| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= select("ticket", "category", options_for_select(CATEGORY))%>
  </div>

  <div class="field">
    <%= form.label :equipment %>
    <%= select("ticket", "equipment", options_for_select(EQUIPMENT))%>
  </div>

  <div class="field">
    <%= form.label :customer_id %>
    <%= select("ticket","customer_id", options_for_select(customer_array)) %>
  </div>

  <div class="field">
    <%= form.label :hardware %>
    <%= form.text_field :hardware %>
  </div>

  <div class="field">
    <%= form.label :software %>
    <%= form.text_field :software %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.hidden_field(:status) %>
  </div>

  <div class="field">
    <%= form.label :walk_in %>
    <%= form.check_box :walk_in %>
  </div>

  <div class="field">
    <%= form.hidden_field(:user_id) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
